# Localist Framework backend dependencies
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Settings management (.env file support)
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# HTTP client (used by FoundryRuntimeClient)
requests>=2.31.0

# Pydantic v2 (FastAPI pulls this in; pin for stability)
pydantic>=2.7.0
python-multipart==0.0.32

# Wiki document parsing (frontmatter + [[link]] extraction)
PyYAML>=6.0

# EmbeddingEngine — local corpus/episodic-memory embeddings (embedding_engine.py).
# Pulls in mlx, mlx-vlm, huggingface-hub, and transformers transitively. Was
# previously undeclared here (only ever present incidentally via other MLX
# packages), so a clean venv would silently import-fail and MemoryManager
# would fall back to keyword-only scoring with no cosine-similarity retrieval.
# First run downloads mlx-community/embeddinggemma-300m-4bit (~400MB) from
# the Hugging Face Hub and caches it locally — requires internet access once.
mlx-embeddings>=0.1.0

# localist-mcp service — HTML extraction (fetch_url tool; ported from the
# retired standalone Fetcher microservice, Phase 2)
lxml>=6.1.1
readability-lxml>=0.8.4.1
chardet>=7.4.3
cssselect>=1.4.0
httpx>=0.28.1

# localist-mcp service — MCP (Model Context Protocol) SDK, SSE transport
mcp>=1.28.1

# localist-mcp service — generate_chart tool (server-side PNG rendering)
matplotlib>=3.9.0

# System memory introspection — context_profile.py's startup RAM headroom
# check (local backends only), and diagnostics/diag_local_working_memory_ram_probe.py
psutil>=7.0.0