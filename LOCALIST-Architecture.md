# Localist Framework — Canonical Architecture Specification

> **Status: Authoritative**
> This document is the canonical reference for Localist Framework's substrate architecture.
> No implementation begins until it is reflected here. No deviation from this
> specification is made without updating this document first.

> **This file is now an index.** Full section content lives in
> `docs/architecture/NN-*.md` (one file per numbered section below). Retired
> sections live in `docs/architecture/archive/`. Read this index first; only
> open the specific section file you need. Edits to substance go in the
> section file, never back into this index — see "Keeping this index current"
> at the bottom.

---

## Core Constraints

Localist Framework is a **local-first, agentic general assistant**. Every architectural
decision is evaluated against five constraints:

| Constraint | Meaning |
|---|---|
| **Local** | All inference, embeddings, memory, and tools run on-device. No cloud calls except explicit user-initiated web search or page fetch. |
| **Sparse** | Memory is high-value semantic events, not transcripts. Prompts carry only what is needed. |
| **Predictable** | The same input produces the same routing decision. Inference is used for reasoning, not for control flow, except where explicitly specified. |
| **Minimal** | System prompts are small. Persona lives in the wiki. Agents are single-purpose. |
| **Auditable** | Every prompt can be logged and read. Every memory write has provenance. Every routing decision has a named rule. |

These constraints are not preferences. They are the identity of the system.

---

## Sections

| # | Section | Status | Last updated | Summary |
|---|---|---|---|---|
| 2 | [Episodic Memory Schema](docs/architecture/02-episodic-memory-schema.md) | Authoritative | 2026-07-23 | Defines the episode as the unit of durable memory — a sparse, typed, provenanced fact extracted from conversation (not a transcript or turn log) — plus its table schema, field reference, type taxonomy, lifecycle rules, retrieval modes, and summarization contract. New 2026-07-23 (§2.12): fixed a live bug where "remember I'm X" (no "that") wrote nothing — two independent gates (`planner._priority2_memory()` and `episodic_extractor.detect_explicit_signal()`) both required the literal "remember that"; fixed with a shared deterministic pattern (`_has_explicit_remember_signal()`) after semantic gating was tried and rejected on measured evidence (write-intent vs. recall-intent collide too much for cosine similarity here, unlike §4.3's Priority 5 fix). Same-day follow-up: closed the "don't forget" retraction collision surfaced above — `_RETRACTION_SIGNALS`' "forget that" substring was matching "don't forget that X" (means "remember") and routing it to delete instead; fixed via `planner._MEMORY_NEGATED_FORGET`, checked before the retraction loop, folded into `_has_explicit_remember_signal()`. Also covers: real cosine-similarity retrieval (Mode 3) merged with recency priming (Mode 2) so all seven episode types are actually reachable, not just the four Mode-2 types; semantic (embedding-based) retraction via `best_match()`/`retract_by_id()`; the `pending` lifecycle status and write-approval gate (§2.11); content-safety scanning on write (§2.10); and the `MEMORY.md` human-readable snapshot (§2.9). |
| 3 | [Unified Prompt Contract](docs/architecture/03-unified-prompt-contract.md) | Authoritative | 2026-07-19 | Specifies the fixed multi-slot prompt layout (current datetime, session files, episodic memory, RAG, tool results, working state, instruction), the aggregate token budget, `PromptBuilder`'s interface, and the ongoing KV-cache / stable-prefix investigation into cache efficiency under the current runtime. Now covers: the unnumbered `[CURRENT DATETIME]` slot (§3.2, Slot DT), added 2026-07-17; and Slot 6 (Working Memory) becoming backend-tier-aware (2026-07-18, see §16.7). Updated 2026-07-19 (§16.11): the local tier's working-memory ceiling is no longer RAM-tiered — it's now `OMLXRuntimeClient.max_model_len − 27,000` (floored at 300), and the local turn-count cap is gone entirely (both tiers are now token-only, `working_memory_limit=None`). Also new 2026-07-19 (§3.7d): `PromptBuilder.build(emit_structured_working_memory=True)` — opt-in, oMLX-only, default-False reproduces prior behavior exactly — omits Slot 6 from the flattened `user_prompt` and instead returns the trimmed working-memory turns as a structured `list[Turn]`, which `OMLXRuntimeClient` now sends as discrete `messages` array entries (mirroring oMLX's own web UI and fulfilling §3.7b's speculative multi-turn direction) instead of flattened text — Ollama/Foundry are completely unaffected. |
| 4 | [Planner Routing Model](docs/architecture/04-planner-routing-model.md) | Authoritative | 2026-07-23 | The deterministic, priority-ordered decision tree that routes each instruction to a tool, retrieval path, or direct-answer route; covers `RoutingPlan`/`ControllerResult` schemas, compound-instruction handling, the tool dispatcher, and Gemma 4B-specific behavioral constraints. New 2026-07-23 (§4.3): Priority 5 (episodic-fetch gate) gained a semantic cosine-similarity fallback alongside its existing keyword check, after a live miss ("Help me prepare for the upcoming Claude Impact Lab...") matched no keyword and skipped episodic retrieval entirely — threshold (0.70) and templates picked empirically against the real tuned embedding model, not guessed (`diagnostics/reports/episodic_relevance_semantic_gate_2026-07-23.md`); also corrected a stale comment claiming `_episodic_injected` short-circuits P5 to always-fire, which hasn't been true since P5 moved off inference. Same-day follow-up: Priority 2 (explicit memory-write gate, `_MEMORY_KEYWORDS`/`_priority2_memory()`) had the identical "remember that"-only gap for a different reason — semantic gating was tried here too and rejected on measured evidence (write vs. recall intent share too much vocabulary for cosine similarity to separate; two rounds both found negative-max > positive-min). Fixed instead with a deterministic pattern, `_has_explicit_remember_signal()` (bare "remember", excluding interrogative prefixes/trailing "?"/"i remember" reminiscing), shared with `episodic_extractor.detect_explicit_signal()`'s independent, identically-narrow gate so both agree — full detail and the rejected-semantic-gating data at `diagnostics/reports/explicit_memory_write_gate_2026-07-23.md` and §2.12 of the Episodic Memory Schema doc. That same fix-pass surfaced (and, per follow-up request, also closed same day) an adjacent higher-stakes bug: `_RETRACTION_SIGNALS`' `"forget that"` substring was colliding with `"don't forget that X"` (means "remember") and routing it to delete — fixed via `_MEMORY_NEGATED_FORGET`, checked before the retraction loop in `detect_explicit_signal()`; full detail in §2 (§2.12). New 2026-07-22: **Priority 3-news** (§4.2) — `_NEWS_KEYWORDS` (`"news"`, `"headlines"`, `"breaking"`, `"top stories"`, etc.), routing to the new `news_search` tool (full design at §14.9) ahead of the generic Priority 3 `web_search` branch; `"news"` moved out of `_WEB_SEARCH_KEYWORDS` into the new set to avoid the two priorities racing on the same word. Also new: §4.6.1's Step 3b corpus-fallback check extended with a third clause (`r.tool_name.startswith("news_search")`) so `news_search`'s Brave-fallback provenance rename doesn't silently escape the check — same gap `research` already hit and fixed the same way. Now covers: an explicit-date P3 tool-gate signal (§4.2, 2026-07-17) closing a live gap where a specific-date query produced no tool grounding, and the resulting empty-completion guard + bounded forced-`web_search` retry (§4.6.2) that stops a zero-content Ollama completion from ever reaching the user regardless of what the Planner gate missed. New 2026-07-20: a third P3 keyword signal, `_CHART_KEYWORDS` (§4.2), gating the new `generate_chart` tool (full design at §14.8); `ResponseMetadata`'s `chart` field (§4.4a), present only on a successful chart dispatch. Same-day follow-up: `_CHART_KEYWORDS` gained `"turn this into a ..."` phrasing after live testing caught a coverage gap against an already-validated diagnostic-corpus instruction (§4.2). Also new 2026-07-20: **Priority 0** (§4.2), an explicit `/chart`/`/research` slash-command bypass ahead of every other priority — `/research` in particular forces the research loop regardless of `LOCALIST_RESEARCH_LOOP_ENABLED` (full detail at §18.2/§18.9), tagged `tool_signal_source="slash_command"`, live-verified end to end for both commands. |
| 5 | [Fetcher Service](docs/architecture/archive/05-fetcher-service-retired.md) | **Retired** (2026-07-03) | 2026-07-03 | The standalone FastAPI microservice (port 8002) that performed URL fetch + readability extraction from project inception through Phase 1. Retired in Phase 2 — its logic was ported in-process into the `fetch_url` MCP tool on `localist-mcp` (§14). Kept for historical reference only. |
| 6 | [Build-Order Checklist](docs/architecture/06-build-order-checklist.md) | Authoritative | 2026-07-09 | The strict, dependency-ordered build phases (memory substrate → prompt contract → planner → controller → episodic extraction → tool dispatcher → final integration) plus running session-progress notes. |
| 7 | [Localist UI](docs/architecture/07-localist-ui.md) | Authoritative | 2026-07-23 | The SvelteKit frontend: routes, provenance bar, episodic memory panel, API proxy, status bar and live-turn/SSE streaming behavior, chat history persistence, and the file browser. New 2026-07-23: §7.15 fixed the Live Feed panel staying stale after "Daily News Brief Refresh" (the panel's store was only ever populated once in `onMount`, which never refires on a client-side route change — fixed by explicitly re-fetching after the refresh call resolves); §7.16 added an "Ask about this" per-article button in the Live Feed panel that sends just that one story into the current conversation for a model-generated detail summary (backend `news_search` URL-pinning at §14.10); §7.17 fixed the Settings "Local area" field appearing permanently stuck on its placeholder text — root cause was the Save button silently requiring an unrelated exactly-3-topics selection before it would persist anything, now surfaced as a disabled-button state with a persistent inline hint instead of a post-click error. Also covers: the "1a — Inline Provenance" desktop UI port (§7.10) — new tokens/light theme, collapsible/resizable sidebar with in-place Chat/Files sub-nav expansion, consolidated single-chip appbar (superseding the prior three-chip layout, §7.6), collapsed-by-default provenance disclosure, card-based Settings, and a real backend-wired "Re-embed Corpus Now" control (§7.10, 2026-07-17) surfacing `POST /memory/reembed` with a `corpus_stale` staleness badge; the Files restructure (§7.11) — listing/upload/ingest moved into the sidebar, plus new file download and two-step delete endpoints/UI; KaTeX math rendering in `MarkdownRenderer.svelte` (§7.12, 2026-07-18) — a deliberate, documented exception to the renderer's prior no-third-party-deps rule, added after live use showed models emitting literal LaTeX source (e.g. `$\rightarrow$`) that the backend prompt pipeline was confirmed innocent of; live-verified by the user against the running chat UI; `ChartRenderer.svelte` (§7.13, 2026-07-20) — Chart.js-based interactive chart rendering for the new `generate_chart` tool (full design at §14.8), colored from this app's own CSS custom properties rather than a separate palette, live-verified end-to-end via a headless-browser driver script; and (§7.14, 2026-07-22) the Daily News Brief UI's header-button-and-hover-popover design retired in favor of a persistent, collapsible right-side "Previews" tab (`previewsPanel.ts`/`PreviewsPanel.svelte`, a new third `#app-shell` grid column) after live use showed the popover too small/truncated for real headlines — plus two real Settings bugs found and fixed along the way: a native browser address-autofill dropdown on the "Local area" field (`autocomplete="off"`), and a genuine Svelte compiler pitfall where mixing a store-subscription read with `bind:value` targets in one reactive block silently disabled typing entirely (root-caused by compiling a minimal repro and reading the generated JS, fixed by replacing the reactive mirror with a one-time `onMount` sync). |
| 8 | [Graph Retrieval Layer](docs/architecture/08-graph-retrieval-layer.md) | Authoritative | 2026-07-23 | The offline wiki link-graph (schema, `wiki_doc.py` shared parser, WikiAgent link validation, resolution rule) plus an extensive dated log of validation runs and open items, several since closed — including a new startup `reconcile_wiki()` mechanism (Open Item 13) that resyncs hand-edited wiki pages and removes orphaned `document_index` rows for deleted files, with a dedicated audit log. §8.9 (2026-07-23): graph now feeds inference, not just write-time — a RAG-assist `[RELATED CONTEXT]` slot surfaces a wiki page's 1-hop neighborhood on ordinary RAG turns, and `episodes` get real `graph_nodes` rows (`node_type="episode"`, synthetic `episode://<id>` doc_path) linked to wiki concepts via the existing P3c stem/token-overlap resolver — no schema migration needed, `node_type` was already free-text. |
| 9 | [Slot 6A — Structured Working State](docs/architecture/09-slot-6a-structured-working-state.md) | Authoritative | 2026-07-06 | The `[WORKING STATE]` prompt slot and its `WorkingMemoryState` dataclass, tracking current project and active artifacts across turns; schema, test suite, and open items (Tier 2 fields, render wiring). |
| 10 | [Semantic Search-Intent Classifier](docs/architecture/10-semantic-search-intent-classifier.md) | Authoritative | 2026-06-28 | Replaces literal-keyword web-search gating with an embedding-similarity classifier, added after a confirmed false-negative incident; covers design decisions, live re-verification, and a running log of threshold-tuning open items. |
| 11 | [Session File Attachments](docs/architecture/11-session-file-attachments.md) | Authoritative | 2026-07-21 | User-uploaded files (and, since 2026-07-21, pinned wiki pages) injected into every subsequent prompt via Slot SF for the session's duration, bypassing planner routing and wiki ingestion entirely; backend cache, API endpoints, UI, and prompt-assembly integration. §11.8: `SessionFile.source` distinguishes an uploaded file from a pinned wiki page, labeled "(from the vault)" in-prompt; `POST /chat/pin-wiki-page` validates against disk, not the graph index. |
| 12 | [Chat History Tab](docs/architecture/12-chat-history-tab.md) | Authoritative | 2026-07-21 | Durable, searchable, user-manageable persistence of chat turns, distinct from both episodic memory and session files; schema, backend API, frontend, and live verification. Open Item 4 closed 2026-07-21: `/history` retired outright (superseded by §20's `/episodes`, retention control already lived independently on `/settings`) — see §20.7. |
| 13 | [Localist CLI](docs/architecture/13-localist-cli.md) (`start_localist.sh`) | Authoritative | 2026-07-06 | The single-lifecycle launcher managing backend, `localist-mcp`, and the UI dev server together (start/stop/log-tail as one unit), including the dev-server-warmup fix for a chat-scroll regression. |
| 14 | [localist-mcp / MCP Tool Layer](docs/architecture/14-localist-mcp-tool-layer.md) | Authoritative | 2026-07-23 | The MCP tool server exposing `fetch_url`, `web_search` (now provider-agnostic — LangSearch default, Brave Search behind `SEARCH_PROVIDER`), `generate_chart`, `news_search`, and file-op tools; `MCPToolDispatcher`, configuration, port topology, and test coverage. New 2026-07-23 (§14.10): `news_search` gained an optional `url` param that pins its NewsAPI results to one already-known article (filtering to the exact URL match and, only in that case, surfacing NewsAPI's previously-discarded `content` field) — built for the new Live Feed "Ask about this" feature (§7.16) so a clicked article's own detail is looked up specifically rather than a fresh query that could resolve to a different story; additive and `None`-default, every existing caller unaffected. New 2026-07-22 (§14.9): `news_search` — a sixth tool, NewsAPI.org-backed, purpose-built for news-shaped queries the generic `web_search` API has no freshness/source concept for, with a tier-2 fallback to the existing Brave-backed `web_search` tool (no new provider integration needed for the fallback) on a NewsAPI miss or error, tagged `tool_name="news_search:brave_fallback"` for provenance. Live-verified against real NewsAPI/Brave traffic: a real query returned dated articles confirming the dev-tier 24h delay doesn't cause false misses, and a forced nonsense-query miss correctly exercised the real fallback chain end-to-end. Full suite 1042 → 1059 passed. New 2026-07-20 (§14.8): `generate_chart` — a fifth tool built diagnostic-first (measured 66.7% chart-argument-extraction MATCH rate on gemma-4-e4b-it-4bit via few-shot prompt + bracket-balanced envelope repair + one temperature-bumped retry, 12.1% residual failure accepted by design), promoted to production as `backend/chart_tool_schema.py`/`backend/json_envelope_repair.py` + `MCPToolDispatcher._run_chart()`, with a deliberate accepted-failure design (a failed chart extraction returns no `ToolResult` at all — not even an error one — so the turn just degrades to prose) that's a documented deviation from every other tool in this file. Live-verified end-to-end through the real running stack (planner gate → extraction pipeline → MCP tool → frontend render), including the degradation path. Same-day follow-up: closed a `_CHART_KEYWORDS` coverage gap live testing caught, and logged a new Open Item 2 repro (§14.7) — a model falsely claiming `"(Source: Tool output)"` for a chart that never rendered while the gap was still open — with a narrow, documented-as-partial mitigation (`controller_agent._strip_false_tool_attribution()`) that does not resolve Open Item 2. |
| 15 | [P6-Fallthrough Classifier & lookup_request Guard](docs/architecture/15-p6-fallthrough-classifier.md) | **Draft** (feature-flagged, not fully live) | 2026-07-08 | Two feature-flagged mechanisms — a shadow-mode classifier for tool-need fallthrough and a resolved-context guard for `lookup_request` — running live for trial data collection but not yet reflected in §4.2's routing tree. |
| 16 | [Runtime Backend Layer](docs/architecture/16-runtime-backend-layer.md) (`BaseRuntimeClient`) | Authoritative | 2026-07-22 | §16.4 further extended (2026-07-22): a live instance hit `sqlite3.OperationalError: table chat_turns has no column named embedding` despite `schema_version=9` already being current — the v8→v9 `ALTER TABLE` never actually ran on that DB, the same class of drift `_check_embedding_provenance()`'s existing self-heal was written for, just on a different table/column. Fixed with a new unconditional self-heal in `_init_db()` (checks/adds the column on every startup, not gated by embedding config, since `add_chat_turn()`'s `INSERT` always names it) — live-verified firing for real via `--reload` against the user's actual running backend, no restart needed. §16.4 extended (2026-07-21), Episode Browsing UI Phase 1: `embedding_provenance` gains a third store, `'chat_turns'` (schema v9 adds an `embedding BLOB` column to `chat_turns`). `add_chat_turn()` now embeds `content` (truncated to 500 chars) whenever `embed_fn` is configured; `'chat_turns'` follows the same never-automatic path as `'corpus'` (arbitrarily large under the `"forever"` eviction preset) via its own `self._chat_turns_stale` flag and `MemoryManager.reembed_chat_turns()` (`POST /memory/reembed-chat-turns`). `MemoryManager.get_chat_turns()` gains `mode="semantic"` (full-table cosine scan, no pre-filter column the way `document_index`'s `token_set` provides one) and `min_score`, exposed via `GET /chat/history?mode=semantic`; falls back to keyword/FTS when `embed_fn` is unavailable or the store is flagged stale, same fail-safe posture as `'corpus'`/`'episodes'`. §16.12 (2026-07-19): `OMLXRuntimeClient` now sends a real multi-turn `messages` array (system message, one entry per working-memory turn, current query last — mirroring oMLX's own web UI) instead of packing everything into one flattened user message, via a new oMLX-only `working_memory_turns` keyword on `infer()`/`infer_stream()` (Ollama/Foundry untouched — Protocol conformance checks member presence, not signatures). Also added distinct handling for oMLX's "Prompt too long" 400 (`server.py`'s `validate_context_window()`): drops the single oldest working-memory message and retries once; a second such 400 raises a clear, distinguishable error rather than looping. §16.11 (2026-07-19): retired the §16.8/§16.9 RAM-tiered `LOCAL_PROFILE` lookup outright (it measured the wrong layer — oMLX manages its own paged KV cache/memory per-machine, independent of host RAM) in favor of budgeting directly off §16.10's `max_model_len`: `working_memory_tokens = max_model_len − 27,000` (floored at 300), `working_memory_limit = None` on both tiers now (the local 5-turn cap is gone). `profile_for()`'s signature changed from `(is_local: bool)` to `(runtime: object)` since the budget now depends on a second live value that can change after a runtime-backend swap. §16.10 (2026-07-19): `OMLXRuntimeClient.health_check()` now also reads oMLX's `max_model_len` field (vLLM-compatible extension to `GET /v1/models`) for the active chat model, caching it onto `self.max_model_len` with an 8192-token conservative fallback when null/missing. §16.9 (2026-07-19): removed `total_context_tokens`/Ollama `options.num_ctx` after `diagnostics/reports/ollama_cloud_num_ctx_findings.md` confirmed it has zero observed effect on Ollama (local or cloud) — the real ceiling is always each model's native context length; corrects §16.7's now-outdated description of `num_ctx` as functional. §16.8 (2026-07-19): investigated whether `LOCAL_PROFILE`'s 300-token/5-turn working-memory ceiling could be safely raised — live-measured on both local backends (oMLX, local Ollama) using real `conversation_log` text and macOS `top` memory-footprint sampling (psutil RSS confirmed to under-report Metal/unified-memory footprint by ~40x on Apple Silicon, a real methodology pitfall now documented in `context_profile.py`). Result at the time: no safe increase (full data: `diagnostics/reports/local_working_memory_ram_findings.md`) — since superseded by §16.10/§16.11's max_model_len-based budget, not re-measured. Added `context_profile.check_local_ram_headroom()`, a startup warn-only guard (mirrors `lifespan()`'s existing reachable/not-reachable pattern) that logs when real headroom is already thin on the active machine, gated on the runtime's `is_local` flag — now purely observability, no longer feeding `working_memory_tokens` (§16.11). The structural-typing `BaseRuntimeClient` protocol and `runtime_factory.create_runtime()` swap-only backend selection; covers the oMLX, MLX-LM/Foundry, and Ollama backends. §16.7 (2026-07-18): every client now exposes a public `is_local: bool`, set once at construction — always `True` for oMLX/Foundry, derived for Ollama from the chat-model tag's `-cloud` suffix (not `base_url`, which is identical for local and cloud Ollama models). This is the single flag `context_profile.py`'s local/cloud `ContextProfile` ceilings key off (Working Memory turn/token caps consumed by `ControllerAgent`/`PromptBuilder`, see §3 Slot 6). §16.4: `OllamaRuntimeClient.embed()` implemented (real `/api/embed`, no longer a stub), the three-tier runtime-backend/MLX/keyword-only embedding-source precedence (platform-gated: MLX `EmbeddingEngine` only attempted on Apple Silicon), and a chat-model fail-fast `ValueError` replacing an unsafe hardcoded default — live-verified end to end with a cloud chat model and a local embedding model sharing one Ollama daemon. §16.4 open item confirmed 2026-07-16: embedding-model-aware thresholds are a real gap, not normal variance (`lookup_request` scored 0.7119 vs 0.578 for the identical utterance across two embedding models) — fixed on the Planner side via a `_TUNED_EMBEDDING_MODEL` guard that disables semantic search-intent gating on a model mismatch rather than silently degrading it, with `main.py` deriving and threading the active embedding model name through every `ControllerAgent` construction site. The identical gap on `MemoryManager`'s corpus/episodic cosine scoring, named as the follow-up above, is now also fixed: a new `embedding_provenance` table (schema v8) tracks which model produced the stored vectors per store (`'corpus'` | `'episodes'`), with a migration-safe seed-not-mismatch rule for pre-existing embedded data and no provenance row. On a genuine mismatch, `'corpus'` is flagged `_corpus_stale` (falls back to keyword-only scoring in `query_corpus()`, cleared only by the new manual `MemoryManager.reembed_corpus()` / `POST /memory/reembed`, since wiki/raw corpora can be large/expensive) while `'episodes'` auto-re-embeds in place at construction time (small, bounded cost) — the split decided for this follow-up. §16.3/§16.5: a real backend-level live switch — `POST /settings/runtime-backend`, per-backend chat-model pinning, `.env` persistence, health-check-before-swap — built and live-verified 2026-07-15 against real oMLX/Ollama/Foundry services, including the in-flight-request-safety claim; the `embed_fn`-staleness concern raised in review turned out to already be correct by construction and was hardened (public `MemoryManager.embed_fn` property, inline comments, two identity-check tests) rather than needing a fix. §16.6: the two remaining open items closed — `_runtime_switch_lock` is now an `asyncio.Lock` (a `threading.Lock` held across `await` was freezing the whole event loop, not just serializing requests, confirmed by a regression test that deadlocks against the old code), and the Settings UI (`localist-ui`, §7.10) is now wired to all three endpoints — live switching, per-backend chat-model dropdown/pinning, and a same-backend-no-op guard, gated by a confirm dialog. |
| 17 | [WikiAgent Diff-Target Path](docs/architecture/17-wiki-agent-diff-target.md) | Authoritative | 2026-07-21 | Standalone `diff_target` context field letting `wiki_agent` propose/apply a diff against an existing wiki page with no raw file involved; Planner Priority 1b routing (reusing P3c's `resolve_graph_target()`); the content-based `apply_unified_diff` hunk-location fix; a review-then-apply chat UI (`POST /wiki/apply-diff`, `metadata.pending_diffs`); and a pre-write snapshot safety net (`wiki/.snapshots/`, 30-day TTL via prune-on-write + startup sweep, unified `wiki_maintenance.log` audit trail) closing the prior no-rollback gap — all live-verified end to end. §17.11 (2026-07-21): closed the "multi-diff turns" open item — investigated while scoping the Episode Browsing UI's diff detail view and found every layer (`WikiAgent` → `_build_wiki_diff_result` → `mark_diff_applied` → `POST /wiki/apply-diff` → `ChatPanel.svelte`'s per-`page_name`-keyed rendering) already operates on the full `pending_diffs` list independently; nothing needed generalizing. Verified, not fixed, with 3 new end-to-end tests (2 diffs sharing one turn, applying one leaves the other's status/on-disk content untouched). §17.10 (2026-07-21): Priority 1c short-circuits diff_target resolution to a single pinned wiki page's stem, folds in a Priority-3 tool signal (e.g. url_fetch) into the same compound plan, and threads the dispatched tool's result into the diff prompt via a new `tool_context` context key. Remaining open item: a confirmed-safe bullet/diff-marker edge case on context lines. |
| 18 | [Research Loop](docs/architecture/18-research-loop.md) | **Draft** (feature-flagged, off by default — but explicitly invokable via `/research` regardless, §18.2) | 2026-07-21 | §18.10 (2026-07-21), Episode Browsing UI Phase 2: `ToolResult` gains a `workflow_id` field; `_run_research_loop()` generates one id per call and stamps it on every `ToolResult` it produces (including the synthetic exhaustion result), so `controller_agent.py` can group them into `metadata["workflow_id"]`/`metadata["workflow_steps"]` (omitted entirely on non-research turns) for the Episode Browsing UI's step-chain view. A bounded search/evaluate/reformulate/fetch cycle (`MCPToolDispatcher._run_research_loop`) that Planner's `research_intent` semantic group upgrades an already-firing `web_search` into, for price/spec-lookup queries a single search snippet can't resolve; gated behind `LOCALIST_RESEARCH_LOOP_ENABLED` (`false` in `.env.example`) and `_RESEARCH_INTENT_THRESHOLD=0.65`. Covers the negative-filter redesign (post-scoring conflict resolution + a documented single-inference-call exception to Planner's no-inference-in-P1-4 rule), the loop's iteration cap/repeat-guard/synthetic-failure-result mechanics feeding `controller_agent.py`'s Step 3b corpus fallback, and the `_RESEARCH_CLASSIFIER_TIMEOUT=15s` override added after a live cloud-side stall. Extensively live-tested against real Brave Search/Ollama Cloud traffic, surfacing and fixing three real bugs (bracket-wrapped URL parsing, a silent-failure NDJSON stream bug in `ollama_runtime_client.py`, and the classifier-timeout tuning) — not yet enabled by default pending more live use. New 2026-07-20 (§18.2): a Priority 0 `/research` slash command (§4.2) forces the loop regardless of the ambient flag — live-verified running with the flag off. Also new 2026-07-20 (§18.9): an 18-query, 7-category live QA pass (`diagnostics/reports/research_loop_qa_assessment_2026-07-20.md`) found the pricing/spec gate's dominant failure mode was accepting any pricing-shaped content as sufficient regardless of relevance to the actual question (0/3 honest-exhaustion rate on deliberately-unanswerable queries); fixed by making `_evaluate_pricing_gate()` relevance-aware (now takes the original instruction, not just the candidate text). Delta re-run on the 6 clearest false positives: 4 improved, 2 unchanged (narrower gate gaps, not regressions), 0 regressed — see §18.8 for the two new open items this surfaced and the QA pass's own scope caveats. |
| 19 | [OKF Wiki Alignment](docs/architecture/19-okf-wiki-alignment.md) | Authoritative | 2026-07-21 | Reconciles the wiki corpus with Google's OKF (Open Knowledge Framework) spec, as adopted by LangChain's OpenWiki 0.2 — closing a pre-existing three-way front-matter drift (`SCHEMA.md`'s placeholder vs. `wiki_agent.py`'s actual `_EXAMPLE` template vs. real pages on disk) rather than adding a fourth inconsistent convention. New `META_WIKI_FILENAMES` shared constant (`wiki_doc.py`) excludes `index.md`/`logs.md`/`MEMORY.md` from corpus indexing, graph nodes, and the wiki-pin picker — also closing a pre-existing `MEMORY.md` exposure gap. `index.md`/`logs.md` are deterministically regenerated in `WikiAgent._finalize()`/`apply_pending_diff()`, never model-authored. `description`/`tags` are model-generated at write time going forward; a one-time `backfill_okf_frontmatter.py` script brought all 7 existing pages up to the new convention. |
| 20 | [Episode Browsing UI](docs/architecture/20-episode-browsing-ui.md) | Authoritative | 2026-07-21 | New three-pane (Filters / Episode list / Episode detail) `/episodes` view treating `chat_turns` as the browsing spine (Path B) and `episodes` as a read-only annotation overlay, rather than extending `VALID_EPISODE_TYPES` (Path A rejected). Backend: `chat_turns` semantic search (schema v9 embedding column, `'chat_turns'` embedding-provenance store, `get_chat_turns(mode="semantic")`, date-range/has-tool-result filters — §16.4/§20.2); a `workflow_id` correlation key stamped on every `ToolResult` a research-loop run produces, surfaced as `metadata["workflow_steps"]` (§18.10/§20.3); verification (not a fix) that multi-diff turns already worked end to end (§17.11/§20.4); a `task_id` filter on `list_episodes()`/`count_episodes()` for the per-turn "related memory" overlay (§20.5). Frontend: new route/store/five components, including `DiffBlock.svelte` extracted from `ChatPanel.svelte` so both call sites share the same (already multi-diff-correct) rendering/apply logic. `/history` retired outright — superseded, no functionality lost (§12.7/§20.7). Backend test suite 987 → 1042 passed; no frontend test framework exists in this repo, verified via `svelte-check`/`vite build` only — no live-browser verification performed (§20.9). |

*Section 1 (System Identity) was folded into "Core Constraints" above — its original 17 lines didn't warrant a standalone file.*

---

## Keeping this index current

When you finish a change to any `docs/architecture/NN-*.md` file:

1. Update that row's **Last updated** date.
2. Re-read the **Summary** cell — rewrite it if the change was material, leave it if cosmetic.
3. Re-check **Status** — move to `Retired` (and relocate the file to `docs/architecture/archive/`) if the change makes the section dead; otherwise leave as `Authoritative` or `Draft`.

Do not add substantive content to this file directly — it stays index-sized so it's cheap to read in full every session.

---

*End of Localist Framework Canonical Architecture Specification (index)*
